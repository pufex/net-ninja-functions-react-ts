const Home = () => {
    return <main
        className="w-[100%] max-w-[1440px] mx-auto min-h-[calc(100vh - 6rem)] flex flex-column items-center p-8"
    >
        <header className="w-full flex flex-column items-center gap-4">
            <h1 className="text-4xl font-semibold text-black">
                Tutorial Requests
            </h1>
        </header>
        <section className="w-full flex flex-column gap-3"></section>
    </main>
}

export default Home
